#pragma once

/*
 * This file contains the hardcoded json keys used for parsing
 * project files.
 */

#include <string>

namespace birb
{
	namespace keys
	{
		const static std::string version = "version";
		const static std::string scene_name_list = "scene_names";
		const static std::string entity_data = "entity_data";
	}
}
